import { View, Text } from "react-native";

export default function RunViewSession() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 24, fontWeight: "bold" }}>Run View Session Page</Text>
    </View>
  );
}
